[gd_scene load_steps=2 format=3 uid="uid://cme3o5wwqtj2l"]

[sub_resource type="GDScript" id="GDScript_f482t"]
script/source = "extends Sprite2D

## 占位类型
enum PlaceholderType {
	## 炮台
	turret,
	## 敌人
	enemy
}

## 占位类型
@export
var type: PlaceholderType = PlaceholderType.turret

# Called when the node enters the scene tree for the first time.
func _ready() -> void:
	pass # Replace with function body.

# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(_delta: float) -> void:
	pass
"

[node name="Placeholder" type="Sprite2D"]
script = SubResource("GDScript_f482t")
